\documentclass[10pt,a4paper]{article}

\usepackage{bsymb}

\thispagestyle{empty}
\setlength{\parindent}{0pt}

EVBT(sys.print.template TeXPackages)
EVBT(sys.print.template TeXDefinitions)

% EVBT(yms.add.defaults) make sure we have PQR ST xy mn cd in the symbol table.
% EVBT(yms.add.variables height doors)
% EVBT(yms.add.constants int8)

\begin{document}

  \fbox{EVBT(eb.show.formula x:=1+y*5+z/3)}
  \hfill
  \fbox{EVBT(eb.show.formula y::NAT)}
  \hfill
  \fbox{EVBT(eb.show.formula x:|x'=x+1)}

  \fbox{EVBT(eb.show.formula x circ y)}
  \hfill
  \fbox{EVBT(eb.show.formula false => 0=1)}
  \hfill
  \fbox{EVBT(eb.show.formula S/\T)}

  \fbox{EVBT(eb.show.formula x mod 42 < 42)}
  \hfill
  \fbox{EVBT(eb.show.formula FALSE : BOOL)}
  \hfill
  \fbox{EVBT(eb.show.formula bool(x=2):BOOL)}

  \fbox{EVBT(eb.show.formula true & true => true)}
  \hfill
  \fbox{EVBT(eb.show.formula S\/T)}
  \hfill
  \fbox{EVBT(eb.show.formula card(S) >= 0)}

  \fbox{EVBT(eb.show.formula x|->y : S**T)}
  \hfill
  $f \defi g$
  \hfill
  \fbox{EVBT(eb.show.formula x / 2)}

  \fbox{EVBT(eb.show.formula dom(x))}
  \hfill
  \fbox{EVBT(eb.show.formula x <| y)}
  \hfill
  \fbox{EVBT(eb.show.formula x <<| y)}

  \fbox{EVBT(eb.show.formula S >< T)}
  \hfill
  \fbox{EVBT(eb.show.formula {})}
  \hfill
  \fbox{EVBT(eb.show.formula #x.x:NAT&x>0)}

  \fbox{EVBT(eb.show.formula 1 ^ 2)}
  \hfill
  \fbox{EVBT(eb.show.formula FALSE:BOOL)}
  \hfill
  \fbox{EVBT(eb.show.formula x ; y)}

  \fbox{EVBT(eb.show.formula finite(S/\T))}
  \hfill
  \fbox{EVBT(eb.show.formula !x,y.x:NAT&y:NAT=>y+x>=x)}
  \hfill
  \fbox{EVBT(eb.show.formula 5 >= y)}

  \fbox{EVBT(eb.show.formula S <| id)}
  \hfill
  \fbox{EVBT(eb.show.formula 4711 : NAT)}
  \hfill
  \fbox{EVBT(eb.show.formula inter(S))}

  \fbox{EVBT(eb.show.formula INTER x . x:0..20 | NAT)}
  \hfill
  \fbox{EVBT(eb.show.formula 17 : INT)}
  \hfill
  \fbox{EVBT(eb.show.formula (%a.a:NAT|a+1))}

  \fbox{EVBT(eb.show.formula P&Q&R)}
  \hfill
  \fbox{EVBT(eb.show.formula 7 <= 77)}
  \hfill
  \fbox{EVBT(eb.show.formula P <=> Q)}

  \fbox{EVBT(eb.show.formula P=>Q)}
  \hfill
  \fbox{EVBT(eb.show.formula not P)}
  \hfill
  \fbox{EVBT(eb.show.formula P or Q)}

  \fbox{EVBT(eb.show.formula x|->y : S)}
  \hfill
  \fbox{EVBT(eb.show.formula max(S) < 17)}
  \hfill
  \fbox{EVBT(eb.show.formula { x,y . x:0..22 & y:23..44 | x|->(x+y) })}

  \fbox{EVBT(eb.show.formula min(T) <= max(T))}
  \hfill
  \fbox{EVBT(eb.show.formula x:NAT)}
  \hfill
  \fbox{EVBT(eb.show.formula y:NAT1)}

  \fbox{EVBT(eb.show.formula x /= y)}
  \hfill
  \fbox{EVBT(eb.show.formula x /: S)}
  \hfill
  \fbox{EVBT(eb.show.formula {} oftype POW(INT**INT))}

  \fbox{EVBT(eb.show.formula x <+ { 1|->2 } )}
  \hfill
  \fbox{EVBT(eb.show.formula partition(S,{c},{d}))}
  \hfill
  \fbox{EVBT(eb.show.formula x : S +-> T)}

  \fbox{EVBT(eb.show.formula y : S >+> T)}
  \hfill
  \fbox{EVBT(eb.show.formula x:POW(NAT))}
  \hfill
  \fbox{EVBT(eb.show.formula S <<: NAT1)}

  \fbox{EVBT(eb.show.formula S || T)}
  \hfill
  \fbox{EVBT(eb.show.formula ((S ** T) <| prj1)(x|->y)) }
  \hfill
  \fbox{EVBT(eb.show.formula ((S ** T) <| prj2)(x|->y)) }

  \fbox{EVBT(eb.show.formula S +->> T)}
  \hfill
  \fbox{EVBT(eb.show.formula !x.x=x)}
  \hfill
  \fbox{EVBT(eb.show.formula ran(x))}

  \fbox{EVBT(eb.show.formula S |> x)}
  \hfill
  \fbox{EVBT(eb.show.formula S |>> x)}
  \hfill
  \fbox{EVBT(eb.show.formula S <-> T)}

  \fbox{EVBT(eb.show.formula S \ T)}
  \hfill
  \fbox{EVBT(eb.show.formula S <->> T)}
  \hfill
  \fbox{EVBT(eb.show.formula S <<->> T)}

  \fbox{EVBT(eb.show.formula S <<: T)}
  \hfill
  \fbox{EVBT(eb.show.formula S /<<: T)}
  \hfill
  \fbox{EVBT(eb.show.formula S <: T)}

  \fbox{EVBT(eb.show.formula S /<: T)}
  \hfill
  \fbox{EVBT(eb.show.formula S >->> T)}
  \hfill
  \fbox{EVBT(eb.show.formula S --> T)}

  \fbox{EVBT(eb.show.formula S >-> T)}
  \hfill
  \fbox{EVBT(eb.show.formula S <<-> T)}
  \hfill
  \fbox{EVBT(eb.show.formula TRUE : BOOL)}

  \fbox{EVBT(eb.show.formula S -->> T)}
  \hfill
  \fbox{EVBT(eb.show.formula union(S))}
  \hfill
  \fbox{EVBT(eb.show.formula UNION x . x:0..20 | NAT)}

  \fbox{EVBT(eb.show.formula 1..111)}
  \hfill
  \fbox{EVBT(eb.show.formula x,y :| x' = x+y & y' = x-y)}
  \hfill
  \fbox{EVBT(eb.show.formula S +-> POW(1..3))}


\end{document}
